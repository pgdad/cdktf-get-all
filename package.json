{
  "name": "cdktf-get-all",
  "version": "1.0.0",
  "private": true,
  "description": "An example project to show how to use `@cdktf/cli-core` to build a version of `cdktf get` that emits all languages instead of one. This can be useful to build construct libraries, but we are not yet sure if it's the best way to solve this problem.",
  "main": "./dist/index.js",
  "scripts": {
    "start": "rm -rf modules && tsx src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@cdktf/cli-core": "^0.17.1",
    "@cdktf/commons": "^0.17.1",
    "@cdktf/provider-generator": "^0.17.1",
    "fs-extra": "^11.1.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/node": "^20.4.2",
    "tsx": "^3.12.7"
  }
}
